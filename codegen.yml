schema: https://ininderprint-backend-nestjs.cloud-dev.ininderprint.tw/vendor-api/graphql
generates:
  file.graphql:
    plugins:
      - schema-ast
    config:
      includeDirectives: true
# config:
#   # custom GraphQL Scalars to TypeScript types map
#   scalars:
#     Date: string
#     ObjectID: string
#     timestamptz: string
#     uuid: string
